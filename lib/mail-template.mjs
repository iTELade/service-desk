const escape=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const replyLine='Odpowiedz powyżej tej linii';
export function emailHtml({brand='iTELade Desk',title,body,url,key,status}){
  body=String(body||'').replace(/^Odpowiedz powyżej tej linii\n_+\n\n/,'');
  const safe=url&&/^https?:\/\//.test(url)?url:null;
  return `<!doctype html><html lang="pl"><head><meta charset="utf-8"></head><body style="margin:0;background:#eef3f9;color:#172b4d;font-family:Arial,sans-serif"><div style="max-width:640px;margin:24px auto;padding:12px"><p style="font-size:12px;color:#65768c;border-bottom:1px solid #bcc9d9;padding-bottom:12px">${replyLine}</p><div style="background:#12385c;color:white;padding:24px 28px;border-radius:12px 12px 0 0;font-size:20px;font-weight:bold">${escape(brand)}</div><div style="background:white;padding:28px;border:1px solid #dbe4ef;border-top:0;border-radius:0 0 12px 12px">${key?`<p style="color:#0969b8;font-size:13px">${escape(key)}${status?' · '+escape(status):''}</p>`:''}<h1 style="font-size:22px;line-height:1.4">${escape(title)}</h1><div style="font-size:15px;line-height:1.7;white-space:pre-wrap">${escape(body)}</div>${safe?`<p style="margin-top:28px"><a href="${escape(safe)}" style="display:inline-block;background:#0766d9;color:white;text-decoration:none;border-radius:6px;padding:12px 18px">${key?'Otwórz zgłoszenie':'Otwórz iTELade Desk'}</a></p>`:''}</div><p style="font-size:12px;color:#65768c;text-align:center">${escape(brand)} · Wsparcie i komunikacja</p></div></body></html>`;
}
